# 快速部署环境变量配置
# 复制此文件为 .env 并填入你的配置

# === 必填配置 ===
# GitHub API访问令牌 (逗号分隔)
# 在 https://github.com/settings/tokens 创建token，需要 public_repo 权限
GITHUB_TOKENS=ghp_your_token_1,ghp_your_token_2

# === 可选配置 ===
# Gemini模型配置
HAJIMI_CHECK_MODEL=gemini-2.5-flash

# 扫描间隔 (支持: 30m, 1h, 2h, 24h 等)
SCANNER_SCAN_INTERVAL=1h

# 仓库年龄过滤 (天数，只扫描最近N天内有更新的仓库)
DATE_RANGE_DAYS=730

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# Web界面端口
WEB_PORT=8080

# 并发工作线程数
SCANNER_WORKER_COUNT=5

# === 外部同步配置 (可选) ===
# Gemini Balancer同步
GEMINI_BALANCER_SYNC_ENABLED=false
GEMINI_BALANCER_URL=
GEMINI_BALANCER_AUTH=

# GPT Load Balancer同步
GPT_LOAD_SYNC_ENABLED=false
GPT_LOAD_URL=
GPT_LOAD_AUTH=
GPT_LOAD_GROUP_NAME=group1,group2